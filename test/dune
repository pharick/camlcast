; One executable per module under test; they all share the Support fixtures
; and testables. Input (SDL owns the keyboard and mouse state), Framebuffer (a
; wrapper over an SDL texture) and Renderer (per-pixel SDL output) all need a
; live SDL surface, so their pure pieces are tested through Plane, Viewport and
; Palette instead.

(tests
 (names
  test_vec
  test_result_ext
  test_plane
  test_sky
  test_image
  test_transform
  test_room
  test_world
  test_ray
  test_player
  test_viewport
  test_color
  test_texture
  test_palette
  test_engine)
 (libraries raycaster alcotest))
